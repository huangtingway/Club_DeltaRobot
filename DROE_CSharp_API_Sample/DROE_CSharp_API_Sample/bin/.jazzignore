### Jazz Ignore 0
# 將不會確定忽略的檔案與資料夾，但在
# 接受或更新期間可能會修改它們。
# - 忽略內容應包含一份以空格區隔的檔名型樣清單。
# - 每一個型樣皆區分大小寫，並括上大括弧（'{' 和 '}'）。
# - "*" 代表符合零或多個字元。
# - "?" 代表符合單一字元。
# - 型樣清單可能分割成數行，其中，以反斜線
#     結束字行，以定位點開始下一行。
# - core.ignore 中的型樣會阻止確定相同目錄中的
#     相符資源。
# - core.ignore.recursive 中的型樣會阻止確定現行目錄
#     和所有子目錄中的相符資源。
# - core.ignore.recursive 的預設值是 *.class 
# - core.ignore 的預設值是 bin 
# 
# 忽略這個子樹狀結構中的 Shell Script 和隱藏的檔案：
#     例如：core.ignore.recursive = {*.sh} {\.*} 
# 
# 忽略現行目錄中名稱為 'bin' 的資源（但是容許
#  它們位於下列任何子目錄中）：
#     例如：core.ignore = {bin} 
# 
# 附註：如果修改忽略檔案，將不會變更
#     Eclipse 衍生資源的忽略狀態。

core.ignore.recursive= 

core.ignore= \
	{Debug} 